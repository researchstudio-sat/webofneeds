@prefix con:   <https://w3id.org/won/content#> .
@prefix match:   <https://w3id.org/won/matching#> .
# empty because the accepts message chain 'contains' the retracts message chain, causing the accepts message to be dropped. 