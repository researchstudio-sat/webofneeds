# This image is build and pushed to the dockerhub manually (webofneeds/nginx)

FROM nginx

# copy the configuration to the container
COPY nginx.conf /etc/nginx/nginx.conf

# when starting this container mount the path to the won server certificates generated by image "gencert"
# as well as a password file (won-certificate-passwd) which holds the password of the certificate
VOLUME /etc/nginx/won-server-certs