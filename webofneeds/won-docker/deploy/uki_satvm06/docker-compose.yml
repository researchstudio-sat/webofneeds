version: '2'
services:

  # owner server self-signed certificate for communication with wonnode
  gencert:
    restart: always
    build: ../../image/gencert
    environment:
      - "CN=cbi.matchat.org"
      - "PASS=file:/usr/local/certs/out/uki_certificate_passwd_file"
      - "OPENSSL_CONFIG_FILE=/usr/local/openssl.conf"
    volumes:
      - $base_folder/won-server-certs:/usr/local/certs/out/
      - $base_folder/openssl.conf:/usr/local/openssl.conf

  # postgres atom databases for wonnode
  postgres_node:
    restart: always
    build: ../../image/postgres
    environment:
      - "POSTGRES_PASSWORD=$postgres_db_passwd"
      - "PGDATA=/var/lib/postgresql/data/pgdata"
    volumes:
      - $base_folder/postgres_node/data/pgdata:/var/lib/postgresql/data/pgdata
    ports:
      - "5433:5432"

  # wonnode with nginx proxy => https://line.uki.or.at/won
  wonnode:
    restart: always
    build: ../../image/wonnode
    environment:
      - "uri.host=cbi.matchat.org"
      - "uri.prefix=https://cbi.matchat.org/won"
      - "http.port=8443"
      - "db.sql.jdbcDriverClass=org.postgresql.Driver"
      - "db.sql.jdbcUrl=jdbc:postgresql://satvm06.researchstudio.at:5433/won_node"
      - "db.ddl.strategy=validate"
      - "db.sql.user=won"
      - "db.sql.password=$postgres_db_passwd"
      - "CERTIFICATE_PASSWORD=$uki_certificate_passwd"
      - "activemq.broker.port=61626"
      - "client.authentication.behind.proxy=true"
      - "JMEM_OPTS=-XX:+HeapDumpOnOutOfMemoryError"
    ports:
      - "8443:8443"
      - "61627:61626"
    volumes:
      - $base_folder/letsencrypt/certs/live/cbi.matchat.org/fullchain.pem:/usr/local/tomcat/conf/ssl/t-cert.pem
      - $base_folder/letsencrypt/certs/live/cbi.matchat.org/privkey.pem:/usr/local/tomcat/conf/ssl/t-key.pem
      - $base_folder/letsencrypt/certs/live/cbi.matchat.org/t-key-cert.pfx:/usr/local/tomcat/conf/ssl/t-key-cert.pfx
      - $base_folder/letsencrypt/certs/live/cbi.matchat.org/t-keystore.jks:/usr/local/tomcat/conf/ssl/t-keystore.jks
      - $base_folder/won-client-certs/wonnode:/usr/local/tomcat/won/client-certs/
    depends_on:
      - postgres_node

  # postgres atom databases for owner
  postgres_owner:
    restart: always
    build: ../../image/postgres
    environment:
      - "POSTGRES_PASSWORD=$postgres_db_passwd"
      - "PGDATA=/var/lib/postgresql/data/pgdata"
    volumes:
      - $base_folder/postgres_owner/data/pgdata:/var/lib/postgresql/data/pgdata
    ports:
      - "5432:5432"

  # owner with nginx proxy (https://line.uki.or.at/owner)
  # TODO change the node uri from node.matchat.org/won to the new meicogsci node url once it is there
  owner:
    restart: always
    build: ../../image/owner
    environment:
      - "node.default.host=cbi.matchat.org"
      - "uri.host=cbi.matchat.org"
      - "http.port=8082"
      - "node.default.http.port=443"
      - "uri.prefix=https://cbi.matchat.org"
      - "uri.prefix.node.default=https://node.matchat.org/won"
      - "db.sql.jdbcDriverClass=org.postgresql.Driver"
      - "db.sql.jdbcUrl=jdbc:postgresql://satvm06.researchstudio.at:5432/won_owner"
      - "db.ddl.strategy=validate"
      - "db.sql.user=won"
      - "db.sql.password=$postgres_db_passwd"
      - "CERTIFICATE_PASSWORD=$uki_certificate_passwd"
      - "owner.webapp.rememberme.key=$rememberme_key"
      - "email.from.won.user=$MAIL_USER"
      - "email.from.won.password=$MAIL_PASS"
      - "email.from.won.smtp.host=$MAIL_HOST"
      - "JMEM_OPTS=-XX:+HeapDumpOnOutOfMemoryError"
    ports:
      - "8082:8443"
    volumes:
      - $base_folder/custom_owner_skin:/usr/local/tomcat/webapps/owner/skin/current/
      - $base_folder/won-server-certs:/usr/local/tomcat/conf/ssl/
      - $base_folder/won-client-certs/owner:/usr/local/tomcat/won/client-certs/
    depends_on:
      - postgres_owner
      - gencert

  # bigdata rdf store for matcher service (this image is not built at deployment, it is downloaded from dockerhub)
  bigdata:
    restart: always
    build: ../../image/bigdata
    volumes:
      - $base_folder/bigdata/data/:/usr/src/bigdata/data
    ports:
      - "10000:9999"

  # matcher service connect with wonnode on line.uki.or.at and node.matchat.org
  matcher_service:
    restart: always
    build: ../../image/matcher-service
    environment:
      - "node.host=satvm06.researchstudio.at"
      - "matcher.uri=https://cbi.matchat.org/matcher_service"
      - "cluster.seedNodes=satvm06.researchstudio.at:2561,satvm06.researchstudio.at:2562"
      - "uri.sparql.endpoint=http://satvm06.researchstudio.at:10000/blazegraph/namespace/kb/sparql"
      - "wonNodeController.wonNode.crawl=https://cbi.matchat.org/won/resource,https://node.matchat.org/won/resource" # crawl uki won node and matchat won node
      - "cluster.local.port=2561"
      - "JMEM_OPTS=-XX:+HeapDumpOnOutOfMemoryError"
    ports:
      - "2561:2561"
    volumes:
      - $base_folder/won-client-certs/matcher_service:/usr/src/matcher-service/client-certs/
    depends_on:
      - bigdata
      - wonnode

  # solr server as an atom index
  solr:
    restart: always
    build: ../../image/solr
    volumes:
      - $base_folder/solr/won/data:/opt/solr/server/solr/won/data
      - $base_folder/solr/wontest/data:/opt/solr/server/solr/wontest/data
    ports:
      - "8984:8983"

  # solr matcher and connect to matcher service
  matcher_solr:
    restart: always
    build: ../../image/matcher-solr
    environment:
      - "node.host=satvm06.researchstudio.at"
      - "cluster.seedNodes=satvm06.researchstudio.at:2561,satvm06.researchstudio.at:2562"
      - "cluster.local.port=2562"
      - "matcher.solr.uri.solr.server=http://satvm06.researchstudio.at:8984/solr/"
      - "matcher.solr.uri.solr.server.public=http://satvm06.researchstudio.at:8984/solr/"
      - "JMEM_OPTS=-XX:+HeapDumpOnOutOfMemoryError"
    ports:
      - "2562:2562"
    depends_on:
      - solr
      - matcher_service

  # mongodb for persistent debug bot
  mongodb:
    restart: always
    image: tutum/mongodb:3.2
    environment:
      - "MONGODB_USER=won"
      - "MONGODB_DATABASE=uki_bot"
      - "MONGODB_PASS=$mongo_db_passwd"
    ports:
      - "27017:27017"
      - "28017:28017"
    volumes:
      - $base_folder/mongodb/data/db:/data/db

  # debug bot used to test atom communication, connect to wonnodes (proxied: line.uki.or.at)
  debug_bot:
    restart: always
    build: ../../image/bots
    environment:
      - "uri.prefix.owner=https://cbi.matchat.org/debug_bot"  # set this for the trust store alias
      - "node.default.host=cbi.matchat.org"
      - "node.default.http.port=443"
      - "uri.prefix.node.default=https://cbi.matchat.org/won"
      - "won.node.uris=https://cbi.matchat.org/won/resource"
      - "botContext.impl=mongoBotContext"
      - "botContext.mongodb.user=won"
      - "botContext.mongodb.pass=$mongo_db_passwd"
      - "botContext.mongodb.host=satvm06.researchstudio.at"
      - "botContext.mongodb.port=27017"
      - "botContext.mongodb.database=uki_bot"
    depends_on:
      - wonnode
      - mongodb


  matcher_sparql:
    restart: always
    build: ../../image/matcher-sparql
    image: webofneeds/matcher_sparql:int
    environment:
    - "node.host=satvm06.researchstudio.at"
    - "cluster.seedNodes=satvm06.researchstudio.at:2561,satvm06.researchstudio.at:2562"
    - "cluster.local.port=2563"
    - "matcher.sparql.uri.sparql.endpoint=http://satvm06.researchstudio.at:10000/blazegraph/namespace/kb/sparql"
    - "matcher.sparql.uri.public=http://satvm06.researchstudio.at/sparql/"
    - "JMEM_OPTS=-XX:+HeapDumpOnOutOfMemoryError"
    ports:
    - "2563:2563"
    depends_on:
    - matcher_service
    volumes:
    - $base_folder/won-client-certs/matcher_sparql:/usr/src/matcher-sparql/client-certs/
